CREATE SCHEMA bible;        -- canonical text & structure
CREATE SCHEMA lookup;       -- enums, codes, standards
CREATE SCHEMA users;        -- highlights, notes, activity
CREATE SCHEMA nlp;          -- derived linguistic artifacts | applied annotations, inference, workflows
CREATE SCHEMA audit;        -- system history

CREATE SCHEMA language;     -- linguistic systems & lexicons
CREATE SCHEMA entity;       -- people, places, objects, events | asserted real-world things & relationships

CREATE SCHEMA geo;          -- spatial data & mappings
CREATE SCHEMA measure;      -- units, systems, conversions
CREATE SCHEMA structure;    -- chronology, harmony, outlines

CREATE SCHEMA standards;
CREATE SCHEMA stepbible;    -- source texts, metadata, from STEPBible, etc.
CREATE SCHEMA morphology;