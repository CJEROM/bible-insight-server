First process all of the morphology codes and ingest them into the database

Then start generating all of the related data e.g. features and feature_values

For derived data the code will be None so instead it will be reading from the 