This will be responsible for interaction between the web app and the server, so require a understanding of both components to work correctly.

To start the api server in the services folder:
`python3 -m api.api`

Currently to allow for running this API I have created a fire wall in advanced settings in Windows Security to open up port 8000 and allow external connections through it.